# CLAUDE.md

Project context for Claude Code. This file intentionally stays short and imports
the shared rules rather than duplicating them.

@AGENTS.md
@docs/TESTING.md
@docs/QA_SCOPE.md

Start with `./scripts/qa-bootstrap.sh`, then `make qa-fast`. Follow the hard
limits in `AGENTS.md`. When a data/behaviour contract is ambiguous, ask the
project owner instead of guessing.
