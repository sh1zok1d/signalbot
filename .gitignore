.env
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.DS_Store
soak-test-*.log
soak-test-*.meta

# local DB backups (deploy/backup.sh)
backups/

# Secrets
.env
.env.*
!.env.example
*.pem
*.key
# rclone remote credentials belong in /etc/signalbot or ~/.config/rclone on
# the VPS (see docs/DATA_DURABILITY_RUNBOOK.md), never in this repo — ignored
# defensively in case one is ever dropped into a working tree by mistake.
rclone.conf
backup.env

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Runtime / local research data
logs/
data/
backups/
artifacts/
*.log

# OS / IDE
.DS_Store
.vscode/
.idea/
