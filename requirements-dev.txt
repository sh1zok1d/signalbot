# Development/test-only dependencies, layered on top of the production
# requirements.txt (never merged into it -- CI/local dev tooling has no
# business being installed in a production deploy). Install with:
#
#   pip install -r requirements-dev.txt
#
# The CORE_BTC_BINANCE_V0 materializer needs pyarrow. A research VPS that
# is not running full CI/dev extras should install requirements-research.txt
# instead (same pyarrow pin; not a production dependency).
#
-r requirements.txt

pyarrow==17.0.0

# Test runner used explicitly by .github/workflows/ci.yml.
pytest==8.3.5
