"""Research-only scripts (MATH-00x style investigations). Never imported by
production runtime code (`main.py`, `runtime/`, `backfill/`, `analytics/`,
`storage/`) -- see `tests/analytics/test_isolation_*` conventions elsewhere
in this repo for the same posture applied to Stage2/V2 isolation."""
